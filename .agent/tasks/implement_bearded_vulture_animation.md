Task: Implement Bearded Vulture Animation
Status: Completed

Details:

- Updated `specialAction.js` to return `cavedCards`.
- Updated `game.js` to generate `CAVE_CARDS` event when picking cards to cave.
- Leveraged existing `CAVE_CARDS` UI logic in `game.wxml`.

Associated Files:

- miniprogram/utils/specialAction.js
- miniprogram/pages/game/game.js
